services:
  db:
    image: postgres:15
    container_name: smartfarm-db-prod
    env_file:
      - .env.prod
    volumes:
      - postgres_prod_data:/var/lib/postgresql/data

  api:
    image: smartfarm-api:${IMAGE_TAG}
    container_name: smartfarm-api-prod
    env_file:
      - .env.prod
    depends_on:
      - db
    ports:
      - "8002:8000"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"
        ]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  postgres_prod_data:
